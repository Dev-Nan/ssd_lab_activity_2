for question 1

I took Count variable initialise it to 0, i.e. Count=0;

then i incremented it till NR;
now i took a variable idx and store middle value in it

In End i took a for loop iterated it from i=0 to NR and if i==idx the print $0;